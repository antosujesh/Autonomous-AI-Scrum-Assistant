<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= typeof appSettings !== 'undefined' ? appSettings.AppName : 'Follow-up AI' %> — WhatsApp Task Management Agent">
    <title><%= pageTitle %> | <%= typeof appSettings !== 'undefined' ? appSettings.AppName : 'Follow-up AI' %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
    <link rel="stylesheet" href="/main.css?v=10">
</head>
<body>
    <div class="app-container">
        <%- include('sidebar') %>
        <main class="main-content">
            <%- include('header') %>
            <div class="content-body">
